server:
  port: 8082

spring:
  application:
    name: user-service
  datasource:
    url: jdbc:postgresql://localhost:5432/user_service
    username: skillsync
    password: skillsync123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

jwt:
  secret: ${JWT_SECRET:mySecretKeyForSkillSyncPlatformThatIsLongEnough}

supabase:
  url: ${SUPABASE_URL:https://ymihlmgnwmmbjlyaxzii.supabase.co}
  key: ${SUPABASE_KEY:eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InltaWhsbWdud21tYmpseWF4emlpIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NjMwMTYwNzIsImV4cCI6MjA3ODU5MjA3Mn0.VMx0dnqSisFUItjJEhvimxo-Fgq_Rw46O6TMcC0kpFU}
  storage:
    bucket: profile-images

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.skillsync.user: DEBUG

---
spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://postgres:5432/user_service